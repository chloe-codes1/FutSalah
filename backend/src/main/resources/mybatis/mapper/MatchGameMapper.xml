<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ido.arduino.mapper.MatchDto">


	<!-- 검색 조건에 모두 맞는 내용 찾기 -->
	<select id="alloption" resultType="match">
		select m.date , m.time, m.location, m.isBooked, c.name from matching
		m, left outer join court c
		<where>
			locationID =#{locationID}
			<if test="date !=null ">
				and date LIKE CONCAT('%',#{date},'%')
			</if>
			and time=#{time}
			and isBooked =#{isBooked}
			and courtID =#{courtID}
		</where>
	</select>


	<!-- 검색 조건중 위치, 날짜 맞는 내용 찾기 -->

	<select id="simpleoption" resultType="simple">
		select * from matching
		where locationID =#{locationID}
		and date LIKE
		CONCAT('%',#{date},'%')
	</select>


	<!-- 검색한 조건 없으면 매칭 등록 -->
	<insert id="insertmatch" parameterType="match">


	</insert>


	<!-- 나에게 온 매칭 정보  -->
	
	
	<!-- 내가 신청목록에 올려둔 매칭 정보  -->




</mapper>